follow instructions here : 
https://docs.nginx.com/nginx/admin-guide/installing-nginx/installing-nginx-docker/#docker_plus


to create & deploy a trial version of nginx+ to docker

docker build --no-cache -t ag-nginxplus-from-scratch .

docker run --name ag-nginxplus -p 80:80 -d ag-nginxplus-from-scratch

docker exec -it ag-nginxplus /bin/bash
